services:
  sankt-georg-app:
    build: .
    image: sankt-georg-app:latest
    container_name: sankt-georg-app
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Berlin
    expose:
      - "5003"
    networks:
      - web
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL

networks:
  web:
    external: true
